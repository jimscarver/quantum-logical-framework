# Emergent Gravity in QLF:
# Radial Signed Bias from Primitive Folds

In QLF, folds are primitive. We do not add gravity as a separate force, and we do not begin with a pre-existing spacetime container. We begin with admissible QuCalc folds and accept what stable closure generates.

Gravity is the radial image of uncanceled QuCalc spatial action.

The same primitive branch logic that images transversely as magnetism and electricity images radially as gravity. What persists through admissible closure appears in our world as curvature, acceleration, potential, and effective coupling.

This file states that view directly and without apology.

---

## 1. Primitive basis

QLF begins from the canonical 8-twist alphabet:

```text
^ v   < >   / \   + -
```

A history is physically admissible when it respects the canonical QuCalc branching law. A stable event is an admissible history whose net action closes to Zero Free Action (ZFA).

Write the action of a history `H` as:

`A(H) = (v, h, d, l)`

where:

- `v` = net vertical action
- `h` = net horizontal action
- `d` = net depth action
- `l` = net local or gauge action

A stable closure satisfies:

`A(H) = (0, 0, 0, 0)`

That is the starting point. QLF does not seek deeper primitives behind the folds. The folds are the primitives.

---

## 2. The common microscopic branch pair

The current QuCalc implementation uses the same primitive branch contrast for electromagnetism and gravity:

- expansion branch: `^>` with `^>` → `+1` signed spatial interval unit
- contraction branch: `v>` with `^>` → `-1` signed spatial interval unit

These are not decorative symbols. They are the minimal logical events from which spatial imaging emerges.

In the transverse reading:

- residual signed bias images as magnetism
- momentum exchange along the carrier axis images as electricity

In the radial reading:

- residual signed bias images as gravity

So the framework does not begin by postulating three unrelated interactions. It begins with one QuCalc branch algebra and accepts the different geometric images generated by that algebra.

---

## 3. Gravity as radial image

Gravity is not imported from Newton or Einstein. It is generated from the radial reading of uncanceled spatial action.

The pure branches are:

### Radial expansion

`^>` with `^>` gives:

- positive signed interval unit
- outward logical volume twist
- positive radial distance image
- positive radial momentum image

### Radial contraction

`v>` with `^>` gives:

- negative signed interval unit
- inward logical volume twist
- negative radial distance image
- negative radial momentum image

Neither branch alone is “the gravitational field” we observe macroscopically. What we observe is the surviving imbalance between them.

Gravity is therefore:

> the residual radial signed bias of admissible QuCalc folds

That is the whole statement in native form.

---

## 4. Microscopic scale

For wavelength `λ`, the current file uses the same basic cycle accounting as `magnetism.py`.

Momentum exchange per thread per cycle:

`p_cycle = h / λ`

Cycle period:

`t_cycle = λ / c`

So the radial force image per thread is:

`F_radial = (signed_interval_units × h / λ) / (λ / c)`

which simplifies to:

`F_radial = signed_interval_units × hc / λ²`

This is not an added force law. It is the direct image of the signed interval branch when carried through one cycle.

So the microscopic meaning is simple:

- the fold determines the sign
- the wavelength sets the cycle scale
- the force image is what that signed fold produces per cycle

---

## 5. Domain gravity

Macroscopic gravity is not the image of one isolated fold. It is the image of many logical threads whose radial branches do not perfectly cancel.

Each domain is described by:

- expansion percentage
- contraction percentage

Define the normalized fractions:

`f_exp`
`f_con`

Then the net radial bias is:

`b_rad = f_exp - f_con`

This bias is the gravitational source in the current implementation.

If `b_rad = 0`, radial expansion and contraction cancel and no net gravitational image survives.

If `b_rad > 0`, the domain images as net outward radial bias.

If `b_rad < 0`, the domain images as net inward radial bias.

So observed gravity is not mysterious attraction at a distance. It is the large-scale image of incomplete cancellation among primitive radial folds.

---

## 6. Flux, acceleration, potential, and coupling

Once the net radial bias survives, the file reports its standard large-scale images.

With thread count `N` and unit microscopic force scale `hc / λ²`:

`F_net = b_rad × N × hc / λ²`

From that the file forms a radial acceleration image by dividing by unit mass:

`a_rad = F_net / 1`

and then a total potential-like quantity over the travel interval:

`ΔΦ = a_rad × num_cycles × travel_distance`

The file also reports an effective Newtonian coupling:

`G_eff = |b_rad| × (hc / λ²) × (aperture / thread_count)`

The meaning is direct:

- QuCalc fold imbalance generates net radial flux
- net radial flux images as force
- force images as acceleration
- acceleration integrated over travel images as potential
- aggregated bias images as effective coupling

This is why the file is called `gravitational_tensor.py`: it is organizing the radial image of fold imbalance into the components later described by gravitational language.

---

## 7. Relation to electromagnetism

The unification claim is simple.

The same primitive branch algebra supports all three observed images:

- transverse residual bias → magnetism
- carrier-axis momentum exchange from transverse bias → electricity
- radial residual bias → gravity

So QLF does not say that forces are added afterward to logic. It says that what physics calls interactions are geometric images of admissible logical folding.

This is the central repo statement:

> One QuCalc branch algebra, read in different geometric channels, images as the interactions observed in our world.

---

## 8. Energy convention

The current implementation uses the same energy convention as `magnetism.py`.

Total logical bits are counted from the absolute net signed flux threads over the full travel, and total energy is reported as:

`E = h × total logical bits`

That is the present repo convention and should be read as part of the QuCalc-native accounting used by the current files.

---

## 9. What the current implementation states

The present `gravitational_tensor.py` states the following without qualification inside QLF:

- gravity is the radial image of uncanceled QuCalc spatial action
- expansion and contraction are primitive logical volume branches
- the same microscopic branches that seed electromagnetism also seed gravity
- domains of many logical threads produce net radial bias
- that net radial bias images as effective force, acceleration, potential, and coupling
- what we observe macroscopically is the surviving image of generated QuCalc structure

This is the repo’s current gravity model.

---

## 10. Sample reading of the output

A pure radial expansion branch should be read as:

- outward volume bias
- positive distance image
- positive momentum image
- positive radial contribution

A pure radial contraction branch should be read as:

- inward volume bias
- negative distance image
- negative momentum image
- negative radial contribution

A mixed domain should be read as:

- exact balance → no surviving gravitational image
- expansion-dominant domain → net outward gravitational image
- contraction-dominant domain → net inward gravitational image
- larger surviving imbalance → stronger effective coupling and potential image

So what we observe is not a single fold. It is the image of many folds whose radial imbalance has survived closure.

## Gravity, Entropy, and the Causal Frontier

In QLF, gravity is not caused by a primitive force. It emerges from **deterministic Zero Free Action (ZFA) events**. Each admissible QuCalc fold contributes radial expansion or radial contraction, and stable reality consists only of histories that close under Hermitian adjoint with zero net free action.

The entropy relevant to gravity is the entropy of **unresolved information beyond the local causal frontier**. That hidden continuation space represents admissible possibilities not yet resolved within the local light cone. When the radial effects of ZFA events do not cancel perfectly, the residual radial bias appears macroscopically as gravitational effect.

So the QLF statement is:

- **Microscopically:** gravity emerges from pure quantum-logical ZFA events  
- **Coarse-grained:** gravity is strengthened by entropy associated with information outside local causal access  
- **Macroscopically:** the surviving radial imbalance appears as curvature and the effective coupling \(G\)

In this view, gravity is entropic only at the large-scale descriptive level. Its microscopic source remains deterministic quantum-logical closure.

---


## 11. Conclusion

QLF does not begin with gravity as a primitive force.

QLF begins with folds.

Expansion and contraction are primitive logical branches. Admissible closure determines what persists. Residual radial signed bias across logical threads images as gravity. Residual transverse signed bias images as magnetism and electricity. What physics measures is the large-scale image of generated QuCalc structure.

So the native statement is:

> Gravity is the radial image of uncanceled QuCalc spatial action.  
> Expansion and contraction are primitive folds.  
> Their surviving imbalance across logical threads generates the gravitational quantities observed in our world.

That is the present QLF account of gravity.

---

## 12. Next repo improvement

The next improvement should be a short **Sample Run** section copied directly from `gravitational_tensor.py` output, so that the markdown and the code display the same branch logic, domain logic, and emergent gravitational images in one place.
